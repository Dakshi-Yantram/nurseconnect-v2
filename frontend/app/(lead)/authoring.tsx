/**
 * Clinical training leads author content as well as review it, so they get
 * the same module-authoring surface as trainers rather than a separate,
 * half-featured one. Approval still happens on the Review tab — a lead
 * cannot approve a module from inside the editor.
 */
export { default } from '../(trainer)/modules';
